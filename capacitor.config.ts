import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'programacao-para-dispositivos-moveis',
  webDir: 'www'
};

export default config;
